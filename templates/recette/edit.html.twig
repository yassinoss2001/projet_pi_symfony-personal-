{% extends 'FooterHeader.html.twig' %}

{% block s %}
    <title>Modifer Recette</title>
{% endblock %}

{% block body %}

    <main>
        <article>
            <section class="hero" id="home">
    <h1>Modifer Recette</h1>

    {{ include('recette/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_recette_index') }}">back to list</a>

    {{ include('recette/_delete_form.html.twig') }}
    </section>
        </article>
    </main>
{% endblock %}
